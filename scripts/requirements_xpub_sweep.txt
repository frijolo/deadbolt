# Dependencies for xpub_wif_sweep.py
# Only needed if you want faster ECDSA signing via libsecp256k1.
# If not installed, the script falls back to the pure Python implementation (slower but functional).
coincurve>=13.0.0
